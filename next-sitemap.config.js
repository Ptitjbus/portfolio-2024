/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: "https://mathis-viollet.fr",
    generateRobotsTxt: true,
    changefreq: "daily",
    // Autres options de configuration optionnelles
    // priority: 0.7,
    // sitemapSize: 5000,
    // excludes: ['/admin/*', '/private/*'],
};
